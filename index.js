/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import ColorScreen from './src/screens/ColorScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ColorScreen);
